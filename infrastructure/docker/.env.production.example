# Production Environment Variables Template
# Copy this file to .env.production and fill in your actual values

# Database Configuration
DB_NAME=fintech_db
DB_USER=postgres

# Application Configuration
NODE_ENV=production

# API Keys (these will be overridden by Docker secrets in production)
# Only used for local development or when secrets are not available
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_key_here
FINNHUB_API_KEY=your_finnhub_key_here
OPENAI_API_KEY=your_openai_key_here

# Risk Engine Configuration
RISK_CALCULATION_WORKERS=4
SIMULATION_WORKERS=2

# Data Ingestion Configuration
DATA_INGESTION_INTERVAL=300

# AI Model Configuration
AI_MODEL=gpt-4

# IMPORTANT SECURITY NOTES:
# 1. In production, use Docker secrets (defined in docker-secrets.yml)
# 2. Never commit real API keys to version control
# 3. Use strong, unique passwords for all secrets
# 4. Rotate secrets regularly
# 5. Use a proper secret management system for production deployments
